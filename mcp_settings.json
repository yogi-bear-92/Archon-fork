{
  "mcpServers": {
    "archon-mcp": {
      "command": "docker",
      "args": ["exec", "-i", "archon-mcp", "python", "-m", "src.mcp_server.main"],
      "env": {
        "SERVICE_DISCOVERY_MODE": "docker_compose"
      }
    }
  }
}